# Calibration annotation drop location

Place independently completed, schema-valid annotation documents here, separated
by annotator and module. Do not copy hidden metadata, design anchors, another
annotator's labels, or Gold/adjudication output into this directory.

The analysis command intentionally fails when this directory contains no real
annotation records. It never fabricates labels to make a gate pass.
