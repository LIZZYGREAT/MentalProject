# Gate and freeze manifests

Gate results may be written here by the CLI. The immutable
`representation_semantics_v1.0.json` is created only when Gate A and Gate B pass,
the final manual exists, all formal scenarios have adjudicated reference records,
and no construct remains in `REVISE` state.

The freeze command refuses to overwrite an existing versioned manifest.
