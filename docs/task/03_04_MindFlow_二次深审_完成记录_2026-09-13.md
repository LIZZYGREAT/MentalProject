# MindFlow 阶段 03 / 04 二次深审完成记录

## 结论

二次深审提出的三个剩余问题已经全部修复并通过完整回归。

```text
Stage 03 = PASS / freeze
Stage 04 = PASS / freeze
```

原二次深审文件用于描述审查发现和建议，不作为运行时指令。本记录只确认本次实际完成的
代码、测试和提交范围。

## 已完成修复

### 1. Reminder Backend Time Authority

提交：

```text
6523415 fix(reminder): bind persisted time to backend-grounded user time
```

完成内容：

```text
AgentContext 增加 received_at_utc，并由 Worker 使用 BotEvent.create_time 写入
Backend 解析今天/明天/后天、周几、明确日期、中文/数字时钟和相对时长
澄清时间只与最近一条相关 reminder request 组合
Agent remind_at 仅作为 proposal
proposal 与 Backend expected time 不一致时返回 reminder_time_not_grounded
Repository 只写入 Backend expected time
```

### 2. Generic Memory Medication Boundary

提交：

```text
3786056 fix(memory): block medication treatment facts from generic memory
```

完成内容：

```text
拦截服用、用药、停药、换药、加药、减药等治疗行为
拦截明显剂量句式和医生开药句式
补充常见精神科药物回归词
保留早餐、作息、备考等普通日常事实
```

### 3. Research Aggregate Project-Timezone Buckets

提交：

```text
d6eb8e7 fix(research): evaluate aggregate calendar buckets in project timezone
```

完成内容：

```text
ResearchAggregateService 显式接收 settings.timezone_name
自然周/月边界先按项目本地时区构造，再转换为 UTC 查询数据库
DailyReview.local_date 继续使用同一本地日期语义
补充自然周与自然月首尾边界回归测试
```

## 验证

运行环境：

```text
D:\Miniconda\envs\MentalProject
```

最终完整测试：

```text
1498 passed
27 skipped
0 failed
15 warnings（现有依赖弃用警告）
```

## 冻结边界

阶段 03 / 04 不再继续扩大范围。普通 researcher 不自动获得 Admin Web 身份或
`participant_research_detail_read`；Generic Memory 继续不承担临床健康档案职责；
普通自由对话仍不自动生成新的 emotion / valence / arousal 研究特征。
