# Runtime dependencies for local virtual environments and production images.
# Python 3.9+ is required; Python 3.11 is used by the Docker image.
Flask==3.1.2
gunicorn==23.0.0
waitress==3.0.2
matplotlib==3.9.2
numpy==2.0.2
python-dotenv==1.0.1
requests==2.32.5
lark-oapi==1.7.1
snownlp==0.12.3
