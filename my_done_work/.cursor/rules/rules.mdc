---
description:
    项目工程规则（rules）与执行说明
    
    一、项目概述
        - 核心主题：大学生心理健康压力建模
        - 当前阶段：数据读取（通过飞书日历 API 获取数据）
        - 后续阶段：压力建模（依据给定建模思想文档）
        - 目标：以合规、可靠、可复现的方式获取、清洗并存储日历数据，为压力建模提供高质量输入
        - 参考：
            - 飞书日历 API（Calendar V4）：https://open.feishu.cn/document/server-docs/calendar-v4/calendar/primary
            - 压力建模思想文档：https://nankai.feishu.cn/docx/WGySdU3BSoJnpIxUlWjcak7AnSb?from=from_copylink
    
    二、目录结构（关键文件）
        - 根目录
            - GetClassTable.ipynb：示例/实验性 Notebook
            - LARK_tools/：与飞书交互及工具库
                - calendar_tools.py：日历数据读取与处理的核心工具集合
                - CalendarUtils.py：日期/时间与日历相关的辅助函数
                - LarkCal.py / LarkCalRead.py / LarkCard.py：与飞书日历/卡片能力相关的封装
                - userToken.py：令牌与鉴权相关工具（请勿将敏感信息提交到版本库）
                - utils/struct.py：通用数据结构定义
        - .cursor/rules/rules.mdc：本工程规则文档
    
    三、环境与配置
        - Python 版本：建议 3.9+（确保与第三方库兼容）
        - 依赖（示例，按实际项目更新 requirements.txt）：
            - requests 或 httpx：HTTP 调用
            - python-dotenv：加载本地开发环境变量
            - pytz / python-dateutil：时区与日期解析
            - pandas：数据整理（可选）
        - 环境变量（通过系统环境或 .env 提供）：
            - FEISHU_APP_ID：应用 App ID（服务端应用）
            - FEISHU_APP_SECRET：应用 App Secret
            - FEISHU_TENANT_ACCESS_TOKEN：租户级 Token（可通过 AppID/Secret 动态获取）
            - FEISHU_USER_ACCESS_TOKEN（可选）：用户授权 Token（若需代用户拉取）
            - FEISHU_PRIMARY_CALENDAR_ID（可选）：默认主日历 ID
            - REQUEST_TIMEOUT_SECONDS（可选，默认 15）：HTTP 请求超时
        - 安全要求：
            - 严禁将密钥/令牌硬编码至仓库；使用环境变量加载
            - 对令牌打印/日志需做脱敏处理
    
    四、飞书日历 API 使用规范
        - 鉴权流程：
            - 服务端鉴权：使用 AppID/Secret 获取 tenant_access_token
            - 用户鉴权（如需）：OAuth 获取 user_access_token
        - 主要接口与用途（按需使用）：
            - 获取主日历信息：Calendar - Primary
            - 列出事件：List calendar events
            - 拉取忙闲：List free-busy
            - 其他字段/分页/过滤条件参考官方文档
        - 请求规范：
            - 基础 URL、版本与路径必须与官方一致
            - 携带 Authorization 头（Bearer <token>）
            - 遵循分页（page_size、page_token）策略；直到无下一页
            - 时区统一为 UTC 存储，展示层再做本地化
            - 请求重试：对 429/5xx 使用指数退避（最多 3 次，含抖动）
        - 速率限制与退避：
            - 遇到 429（Rate limit）按 Retry-After 或指数退避
            - 记录速率命中日志，后续用于容量评估
    
    五、数据模型与字段标准
        - 统一事件结构（建议在 utils/struct.py 定义）：
            - event_id：字符串，全局唯一
            - calendar_id：字符串
            - start_time_utc / end_time_utc：ISO8601 字符串（UTC）
            - title / description：字符串
            - attendees：数组（对象：user_id/email/role/response_status）
            - source_raw：原始响应的最小必要快照（便于追溯）
        - 关键规范：
            - 保持幂等：以 event_id + calendar_id 作为更新键
            - 时间统一存 UTC；解析入库时校验开始早于结束
            - 对可选字段设置明确的默认值或 None
    
    六、代码规范与异常处理
        - 代码风格：
            - 遵循清晰命名；函数名为动词短语，变量名为名词短语
            - 避免深层嵌套，使用早返回
            - 仅在必要位置添加说明性注释（非显而易见逻辑、边界/性能注意）
        - 日志与可观测性：
            - 级别：DEBUG（开发）、INFO、WARNING、ERROR
            - 记录请求 URL、状态码、耗时（敏感信息脱敏）
            - 标注数据分页进度与重试次数
        - 错误处理：
            - 对可预期错误（鉴权失败、参数错误、429）进行分支处理并返回可操作提示
            - 未知异常抛出前记录上下文（不含敏感数据）
    
    七、开发任务与交付物（阶段划分）
        - 阶段 A：数据读取
            - A1 配置鉴权：实现获取/刷新 tenant_access_token（userToken.py 或新增模块）
            - A2 事件拉取：在 LARK_tools/calendar_tools.py 中实现分页拉取主日历事件
            - A3 数据清洗：按“五、数据模型与字段标准”规范产出统一结构
            - A4 本地落库：CSV/JSON/SQLite 任一，确保幂等与可复现
            - A5 健壮性：完成速率限制、重试、超时与错误处理
            - A6 单元测试：覆盖核心转换逻辑与分页/重试分支
        - 阶段 B：压力建模
            - B1 指标抽取：基于事件（课程/作业/考试/社交）提取候选压力特征
            - B2 建模实现：依据建模文档实现可解释的模型管线（baseline + 可扩展）
            - B3 评估与可视化：输出指标与结果可视化；明确数据分布与异常点
            - B4 文档与说明：在 README 中记录使用方式、参数与注意事项
    
    八、质量保证
        - 测试：
            - 单测：时间解析、字段映射、分页/重试逻辑
            - 集成测试（可选）：对沙箱或模拟服务进行端到端拉取
        - 代码审查：
            - 提交前自检日志与错误处理是否覆盖
            - 确保无敏感信息提交
        - 持续改进：
            - 根据错误日志与速率数据优化分页与重试参数
            - 建模阶段根据验证指标迭代特征工程
    
    九、运行与使用（本地示例）
        - 配置环境变量（或 .env 文件）
        - 在 LARK_tools 目录下运行数据拉取入口（如在 calendar_tools.py 暴露 main）：
            - python LARK_tools/calendar_tools.py --calendar-id <id> --start <iso> --end <iso> --page-size 100
        - 期望输出：
            - 本地生成结构化数据文件（如 data/events.json 或 data/events.csv）
            - 控制台/日志输出分页与条目统计
    
    十、隐私与合规
        - 最小化：仅拉取建模所需字段
        - 脱敏：日志与导出数据对个人识别信息做脱敏或哈希处理
        - 存储期限：仅在建模与评估期间保留；定期清理
    
    十一、变更管理
        - 版本化：遵循语义化提交信息与分支策略（feat/fix/docs/chore/test）
        - 文档同步：代码或接口变更需同步更新本 rules.mdc 与 README
    
    十二、里程碑与验收标准
        - 里程碑 M1（完成数据读取）：
            - 可配置鉴权 + 稳定分页拉取 + 统一数据结构落库
            - 日志可追溯 + 速率限制友好 + 单测通过
        - 里程碑 M2（完成压力建模 v1）：
            - 可复现实验脚本 + 指标产出 + 可视化报告
            - README 完整说明使用方法、参数与结果解释
globs:
alwaysApply: true
---
